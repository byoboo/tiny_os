# Workspace Environment Configuration
# This file ensures the project uses only its local configuration

# Override any global cargo settings
CARGO_BUILD_TARGET=aarch64-unknown-none

# Ensure we're using the local cargo config
CARGO_HOME_LOCAL=1

# Disable global cargo config inheritance for this workspace
CARGO_CONFIG_GLOBAL_DISABLE=1
